int compute(int a, int b)
{
    // int result = (a / a);
    // result *= (b / b);
    // result += (b - b);
    // result /= result;
    // result -= result;
    // return result;

    long x = a + 0;

    long y = x + 2;
}